# 博客内容修改指南

> 想知道网页上某块内容该在哪改？查这份表。
> **所有改完都要重新发布才会上线**，流程见文末第八节。

---

## 一、先分清三个文件各管什么

| 文件 | 管什么 | 改的频率 |
| --- | --- | --- |
| `themes\zzxtx\_config.yml` | **首页所有板块的文字**、导航菜单、页脚 | 最常改 |
| `_config.yml`（在项目根目录） | 博客名、作者、网址、语言、部署设置 | 偶尔改 |
| `source\_posts\*.md` | 文章正文 | 天天改 |

⚠️ **不要改 `_config.landscape.yml`**——那是上一个主题 landscape 的配置，现在完全不起作用。

> 补充一句来历：它是当初用 landscape 主题时按官方做法建的（把主题配置复制一份到根目录，这样主题升级时不会覆盖你的设置）。现在主题换成了自研的 `zzxtx`，这个文件就失效了。
> 因为 `zzxtx` 是你自己写的、放在 `themes\` 里由 git 管理，直接改 `themes\zzxtx\_config.yml` 就行了。如果哪天想把"主题代码"和"我的配置"分开，也可以在根目录建一个 `_config.zzxtx.yml`，Hexo 会用它覆盖主题自带的配置。

---

## 二、总表：网页上看到的位置 → 改哪里

打开 `themes\zzxtx\_config.yml`，对照下表找到对应的配置段。

| 网页上看到的位置 | 配置段 |
| --- | --- |
| 顶部导航菜单（首页 / 项目 / 日志 / 归档 / 关于） | `menu:` |
| 首屏大标题「边学边做，把折腾变成作品。」 | `hero:` |
| 首屏右侧的「当前实验」卡片 | `current_experiment:` |
| 「精选项目」大卡片 | `featured_project:` |
| 精选项目下面那两张小卡片 | `projects:` |
| 「最近实验」文章列表 | **自动生成**，来自 `source\_posts\` |
| 「探索主题」分类 / 标签 | **自动生成**，来自文章的 `categories:` / `tags:` |
| 页脚（GitHub、Built while learning.） | `social:` + `footer:` |
| 左上角 logo 的文字 | `_config.yml` → `author:`（后半截 "/ lab" 是写死在主题里的） |

---

## 三、逐块详解

### 1. 顶部导航菜单

```yaml
menu:
  首页: /
  项目: /#projects
  日志: /#logs
  归档: /archives
  关于: /about
```

- **冒号左边**是菜单上显示的文字，**右边**是点了去哪。
- 想加一个菜单项，照着格式加一行就行（注意和上面缩进对齐，用**两个空格**）。
- 想让「项目」指向别处？把 `/#projects` 换成你的地址即可。
- **`/#projects` 里的 `#` 表示"跳到首页的这个板块"**，不是外部链接。

### 2. 首屏大标题区（Hero）

```yaml
hero:
  eyebrow: "PERSONAL LAB · LEARNING IN PUBLIC"   # 标题上方那行小字
  title_line_1: 边学边做，                        # 大标题第一行
  title_line_2: 把折腾变成作品。                   # 大标题第二行（蓝色字）
  bio: 记录自动化、AI、数据与代码实验。成功的留下方案，失败的留下原因。   # 标题下方的介绍
  main_btn: 查看实验                              # 蓝色主按钮的文字
  main_btn_link: "#logs"                         # 蓝色主按钮点了去哪
  github_btn: GitHub                             # 第二个按钮的文字
  github_link: https://github.com/zzxtx          # 第二个按钮的地址
```

- 大标题分两行显示，`title_line_1` 是黑的，`title_line_2` 是蓝色的。
- 右下角那句「N 篇公开记录，仍在持续更新」**是自动统计的**，文章多了会自己变，不用改。

### 3. 首屏右侧「当前实验」卡片

```yaml
current_experiment:
  label: CURRENT EXPERIMENT   # 卡片顶部小字
  status: ACTIVE              # 右上角绿色徽章
  status_text: 持续开发        # 标题上方的状态文字
  title: 手机每日任务 Agent     # 卡片主标题
  summary: 无线 ADB · 页面识别 · 自动签到   # 主标题下方一行
  progress_label: 稳定性验证    # 进度条左边的说明
  progress: 68                # 进度百分比，填 0-100 的数字
  latest_label: 最新进展        # 下面两格中，左格的标签
  latest: 异常广告绕过          # 左格的内容
  next_label: 下一步            # 右格的标签
  next: 完善 Skill             # 右格的内容
  updated: "2026.09.21"       # 卡片最底部 UPDATED 后面的日期
```

- `progress: 68` 只填数字，**不要写 `%`**（百分号是模板自动加的）。
- 换项目时，整段一起改就行。

### 4. 「精选项目」大卡片

```yaml
featured_project:
  status: 持续开发               # 卡片里的绿色状态徽章
  title: 手机每日任务 Agent       # 大标题
  description: 通过无线 ADB 和视觉判断，让 Agent 自动完成签到、广告任务，并处理不断变化的异常页面。
  tags:                        # 标签，可以多个
    - ADB
    - Agent
    - Automation
  primary_label: 查看 GitHub     # 第一个链接的文字
  primary_link: https://github.com/zzxtx/daily-phone-tasks   # 第一个链接的地址
  log_label: 开发日志            # 第二个链接的文字
  log_link: /tags/个人记录/      # 第二个链接的地址
```

- `tags` 是列表，每一项前面有个短横线 `- `，想加就加一行。
- `log_link` 若指向某个标签页，格式是 `/tags/标签名/`；指向别的页面就写 `/about/` 这种。

### 5. 精选项目下方的两张小卡片

```yaml
projects:
  - title: 股票筛选工具            # 卡片标题
    type: 数据工具                # 左上角小标签
    status: 可用                 # 右上角状态文字
    description: 从筛选脚本延伸到桌面端与 Android 端。
    link: https://github.com/zzxtx/stock-screener   # 点了去哪
  - title: 个人博客主题
    type: Web
    status: 进行中
    description: 为实验日志与项目记录设计的 Hexo 自研主题。
    link: /about/
```

**怎么增加一张卡片**：复制 `- title:` 开始的那 5 行，粘贴到下面，改内容。注意 `-` 要和上面的 `-` 对齐。

**怎么删掉一张**：把它那 5 行整段删掉。

卡片是**两列**排布的，所以写 2、4、6 个最好看。

### 6. 「最近实验」列表 —— 不用手改

这块**完全自动**，显示的是你 `source\_posts\` 里的文章（按日期从新到旧）。想改内容就去改文章本身。

列表下方有个分页阈值：每页显示 10 篇（在根目录 `_config.yml` 的 `index_generator.per_page` 里调）。

### 7. 「探索主题」—— 也不用手改

分类和标签**自动**从文章里收集。你在文章的 front-matter 里写了什么 `categories:` / `tags:`，这里就会出现什么。

---

## 四、写 / 改 / 删文章

文章在 `source\_posts\` 里，一个 `.md` 文件就是一篇。

### 新建

```powershell
cd D:\Blog\my-blog
npx hexo new "文章标题"
```

### 开头必须有这段（front-matter）

```markdown
---
title: 文章标题
date: 2026-09-22 20:00:00
tags: [Hexo, 学习笔记]
categories: 个人记录
---

正文从这里开始写……
```

| 字段 | 作用 |
| --- | --- |
| `title` | 网页上显示的标题 |
| `date` | 发布时间，决定排序 |
| `tags` | 标签，**首页「探索主题」和文章页底部的标签都来自这里** |
| `categories` | 分类，写法可以是 `个人记录` 或 `[个人记录]` |
| `description` | （可选）文章页标题下方的导语，不写就不显示 |

### 想在文章页多显示点信息？

主题还给文章页留了两个可选字段，写上就会显示：

```markdown
---
title: 连接安卓！
date: 2026-09-19 17:41:36
tags: [个人记录]
categories: 个人记录
status: 已记录          # 可选：文章页「状态」那一格显示的字
project: 手机自动化      # 可选：文章页标题上方那行小字（不写就用分类名）
---
```

### 改 / 删

- **改**：直接用 VS Code 打开那个 `.md` 改内容。
- **删**：把那个 `.md` 文件删掉，重新发布后线上那页就没了。
- **改文件名 = 改网址**（因为网址是按文件名生成的）。改名后旧链接会失效。

---

## 五、关于页

改 `source\about\index.md`，就是普通的 Markdown，用 `##` 写小标题、`- ` 写列表。

---

## 六、站点级信息（根目录 `_config.yml`）

| 想改什么 | 改哪个字段 |
| --- | --- |
| 博客名字（浏览器标签页） | `title` |
| 副标题 | `subtitle` |
| 搜索/分享时显示的简介 | `description` |
| 作者名（logo 前半截、页脚都用它） | `author` |
| 语言 | `language` |
| 每页显示几篇文章 | `index_generator.per_page` |
| 文章网址格式 | `permalink` |
| 部署仓库和分支 | `deploy` |

---

## 七、写 YAML 的三条规矩

`themes\zzxtx\_config.yml` 是 YAML 格式，改的时候注意：

1. **缩进只能用空格，不能用 Tab**。同一层的字段左边缘要对齐（本文件用 2 个空格）。
2. **冒号后面必须有一个空格**：`title: 我的项目` ✅ ／ `title:我的项目` ❌
3. **值里出现 `#`、`:`、`"` 时，要用双引号包起来**：
   ```yaml
   main_btn_link: "#logs"        # 正确，# 不加引号会被当成注释
   main_btn_link: #logs          # 错误！这一行会被忽略
   ```

改坏了也不要紧，文件在 git 里有历史，随时能还原。

---

## 八、改完怎么发布

```powershell
cd D:\Blog\my-blog
npx hexo server
```

先在 `http://localhost:4000` 看效果，**不满意就直接改文件、刷新浏览器**（不用重启 server）。确认没问题了再上线：

```powershell
npx hexo clean; npx hexo generate; npx hexo deploy; git add -A; git commit -m "更新内容"; git push
```

等 1 分钟，打开 https://zzxtx.github.io ，如果没变化就按 `Ctrl + F5` 强刷。

---

## 九、进阶：加新板块 / 加新页面

"想加点新东西"分两种情况，难度差很多：

| 你想做的 | 难度 | 要动几个文件 |
| --- | --- | --- |
| 新增一个**独立页面**（有自己的网址，如 `/portfolio/`） | 简单 | 只有配置文件 |
| 在**首页**上加一个新板块 | 进阶 | 配置 + 模板 + 样式，共 3 个 |

---

### 9.1 新增独立页面（建议先拿这个练手）

以加一个「作品集」页面为例，网址是 `/portfolio/`。

**第一步**：创建页面文件

```powershell
cd D:\Blog\my-blog
npx hexo new page "portfolio"
```

会生成 `source\portfolio\index.md`。用 VS Code 打开，写普通 Markdown 就行：

```markdown
---
title: 作品集
---

## 我的作品

- 项目一……
```

**第二步**：加进导航菜单

打开 `themes\zzxtx\_config.yml`，在 `menu:` 下面加一行：

```yaml
menu:
  首页: /
  项目: /#projects
  日志: /#logs
  作品集: /portfolio        # ← 新增
  归档: /archives
  关于: /about
```

**第三步**：走一遍发布流程（第八节）。完事。

> 加「友链」「收藏」「读书笔记」都是这个套路。
> 注意**文件名会成为网址**，所以用英文（`portfolio`），中文标题写在 front-matter 的 `title` 里。

---

### 9.2 在首页加新板块

以加一个「常用工具」板块为例。要改三个文件，**别跳步**。

**① 加配置** —— 打开 `themes\zzxtx\_config.yml`，在**文件最末尾**加上：

```yaml
tools:
  eyebrow: TOOLBOX
  title: 常用工具
  items:
    - name: VS Code
      desc: 写代码用的
      link: https://code.visualstudio.com
    - name: Hexo
      desc: 这个博客的引擎
      link: https://hexo.io
    - name: Git
      desc: 版本管理
      link: https://git-scm.com
```

`items` 下面每 3 行是一张卡片，想要几个就照着复制几段。

**② 加模板** —— 打开 `themes\zzxtx\layout\index.ejs`，找到这一行（全文只有这一处）：

```ejs
<section class="home-section home-shell logs-section" id="logs">
```

在它**上面**另起一行，插入这段：

```ejs
<% if (firstPage && theme.tools && theme.tools.items && theme.tools.items.length) { %>
<section class="home-section home-shell" id="tools">
  <header class="section-head">
    <div><p class="eyebrow"><%= theme.tools.eyebrow || 'TOOLBOX' %></p><h2><%= theme.tools.title || '常用工具' %></h2></div>
  </header>
  <div class="tool-grid">
    <% theme.tools.items.forEach(function(tool) { %>
    <a class="tool-card" href="<%= tool.link || '#' %>">
      <strong><%= tool.name %></strong>
      <span><%= tool.desc || '' %></span>
    </a>
    <% }); %>
  </div>
</section>
<% } %>
```

> 想让卡片在新标签页打开，把中间那行改成：
> `<a class="tool-card" href="<%= tool.link || '#' %>" target="_blank" rel="noopener">`

**③ 加样式** —— 打开 `themes\zzxtx\source\css\style.css`，在**文件最末尾**加上：

```css
.tool-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 12px; }
.tool-card { display: flex; flex-direction: column; gap: 6px; padding: 18px; border: .5px solid var(--border); border-radius: 12px; background: var(--surface); color: var(--text); transition: border-color 180ms ease, transform 180ms var(--ease-out); }
.tool-card strong { font-size: 15px; font-weight: 600; }
.tool-card span { color: var(--text-muted); font-size: 12px; }
@media (max-width: 980px) { .tool-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
@media (max-width: 560px) { .tool-grid { grid-template-columns: 1fr; } }
```

**④ 预览** —— `npx hexo server`，新板块会出现在「日志」上方。满意了再发布。

---

### 9.3 想换成别的内容形式

上面那套是"卡片网格"。换成别的样子，只改 9.2 里 ②③ 两步：

| 想要的样子 | ② 模板怎么改 | ③ 样式怎么改 |
| --- | --- | --- |
| 一段文字，不要卡片 | 把循环体换成 `<p class="tool-text"><%= item.desc %></p>` | `.tool-text { color: var(--text-muted); font-size: 15px; line-height: 1.9; }` |
| 一列条目，竖着排 | 去掉 `<div class="tool-grid">` 这层包裹 | 删掉 `.tool-grid` 的 grid 规则，给 `.tool-card` 加 `margin-bottom: 10px` |
| 两栏图文 | 参考 `index.ejs` 里 `featured-project` 的写法 | 参考 `style.css` 里的 `.featured-project` |

拿不准怎么写，把想要的样子描述给我，我直接给代码。

---

### 9.4 关于"不动主题文件"的 injector

你在 `scripts\custom-inject.js` 里用了 Hexo 的 injector 来挂 `custom.css` / `custom.js`。它能力有边界：

| 能做 | 不能做 |
| --- | --- |
| 在 `<head>` 末尾、`<body>` 末尾追加代码 | ❌ **不能插到页面中间** |
| 挂统计脚本、第三方组件、全局样式覆盖 | ❌ 不能精确定位到某个板块的前后 |

所以：**加首页板块只能改 `index.ejs`**；而"加一段全局 CSS/JS"用 injector 最干净——这正是你现在 `custom.css` / `custom.js` 的用法。

---

## 十、改了没效果？按这个顺序查

1. **有没有重新 `generate` + `deploy`？** 改了文件不重新生成，网页是不会变的。
2. **是不是改了 `_config.landscape.yml`？** 那个文件现在无效，配置在 `themes\zzxtx\_config.yml`。
3. **缩进和冒号空格对不对？** 见第七节。
4. **浏览器缓存？** 按 `Ctrl + F5` 强刷。
5. 还不行就 `npx hexo clean` 清掉缓存再来一遍。
