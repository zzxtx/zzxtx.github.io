# 博客命令行全集

## 开工第一件事（每次都要先做）

```powershell
cd D:\Blog\my-blog
```

> 所有命令都必须在这个目录里执行。之前遇到过的怪问题，绝大多数都是没先 `cd`。

## 一、日常写文章

### 1. 新建文章
```powershell
npx hexo new "文章标题"
```
会在 `source\_posts\` 生成同名 `.md`，用 VS Code 打开写。开头保留这段：

```markdown
---
title: 文章标题
date: 2026-09-19 20:00:00
tags: [标签1, 标签2]
categories: 分类名
---
```

⚠️ 标题里不要出现英文半角冒号 `:`（Windows 文件名非法），用中文冒号 `：` 或短横线。

### 2. 本地预览（写完先看，别急着发）
```powershell
npx hexo server --open
```
自动打开 http://localhost:4000 ，保存后刷新即可。`Ctrl + C` 停止。这一步不联网、不碰 GitHub，随便试。

### 3. 发布上线
```powershell
npx hexo clean
npx hexo generate
npx hexo deploy
```

### 4. 备份源码
```powershell
git add -A
git commit -m "新增：文章标题"
git push
```

## 二、一条龙（日常就复制这四行）

```powershell
cd D:\Blog\my-blog
npx hexo new "文章标题"
# ……用 VS Code 写内容……
npx hexo clean; npx hexo generate; npx hexo deploy; git add -A; git commit -m "新增：文章标题"; git push
```

## 三、修改已发布的文章

不用 `new`，直接改文件，再走同样的发布流程：

```powershell
cd D:\Blog\my-blog
# VS Code 打开 source\_posts\xxx.md 改内容
npx hexo clean; npx hexo generate; npx hexo deploy; git add -A; git commit -m "修改：xxx"; git push
```

改哪个字段影响什么：

| 改的东西 | 网页标题 | 网址 |
| --- | --- | --- |
| 正文内容 | 不变 | 不变 |
| `title:` | **变** | 不变 |
| 文件名 | 不变 | **变** |
| `date:` | 不变 | **变** |

## 四、改配置（站名、菜单、首页板块）

要改的两个文件：

- `_config.yml`（项目根目录）—— 站点信息：`title`、`subtitle`、`description`、`author`、`language`、`timezone`、`url`、`index_generator.per_page`
- `themes\zzxtx\_config.yml` —— 首页各板块的文字、导航菜单、页脚：`menu`、`hero`、`current_experiment`、`featured_project`、`projects`、`social`、`footer`、`toc`、`search`

⚠️ **不要改 `_config.landscape.yml`** —— 那是旧主题 landscape 的配置。站点早就换成自研的 `zzxtx` 了，这个文件现在根本不会被读取，**改了没有任何效果**。

⚠️ 也不要改 `node_modules\` 里的任何文件（`npm install` 会覆盖）。

哪个板块对应哪个字段，见 `改内容指南.md`。

改完**必须全套重来**，这是最容易"改了没效果"的地方：

```powershell
npx hexo clean; npx hexo generate; npx hexo deploy; git add -A; git commit -m "调整配置"; git push
```

## 五、改网址 / 重命名文章

```powershell
# 方法一：在 VS Code 里重命名 source\_posts\旧名.md（网址 = 文件名）
# 方法二：新建时就分开指定标题和网址
npx hexo new "中文标题" --slug my-article
```

改完同样走 `clean → generate → deploy`。

## 六、删除文章

在 VS Code 里删掉 `source\_posts\xxx.md`，然后：

```powershell
npx hexo clean; npx hexo generate; npx hexo deploy; git add -A; git commit -m "删除：xxx"; git push
```

线上那页会跟着消失（`deploy` 是整体覆盖，不留垃圾页）。

## 七、草稿（写完先不想发）

```powershell
npx hexo new draft "草稿标题"     # 存进 source\_drafts\，不会发布
npx hexo publish "草稿标题"       # 转成正式文章，移进 _posts
```

想本地预览草稿：把 `_config.yml` 里的 `render_drafts` 临时改成 `true`，看完记得改回 `false`。
（你这版 Hexo 的 `hexo server` 不支持 `--draft` 参数，别去试。）

## 八、查询与排查

```powershell
git status                                          # 有哪些改动没提交
git log --oneline -5                                # 最近 5 次提交
git -C D:\Blog\my-blog rev-parse --show-toplevel    # 确认自己在哪个仓库
Test-Path D:\Blog\my-blog\.deploy_git\.git          # 必须返回 True，否则先别 deploy
```

网络失败（`Recv failure` / `Could not connect`）就是重试，多试几次通常能过：

```powershell
git push
npx hexo deploy
```

开了代理时临时走代理：

```powershell
git config --global http.proxy http://127.0.0.1:7890
git config --global --unset http.proxy
```

## 九、换电脑恢复

```powershell
git clone -b source https://github.com/zzxtx/zzxtx.github.io.git my-blog
cd my-blog
npm install
npx hexo server --open
```

## 十、三条铁律

1. 每条命令前先 `cd D:\Blog\my-blog`
2. 不要删 `D:\Blog\my-blog\.deploy_git\.git`
3. 不要在 `my-blog` 根目录执行 `git init`

## 十一、速查表

| 目的 | 命令 |
| --- | --- |
| 新建文章 | `npx hexo new "标题"` |
| 新建草稿 | `npx hexo new draft "标题"` |
| 草稿转正式 | `npx hexo publish "标题"` |
| 本地预览 | `npx hexo server --open` |
| 清缓存 | `npx hexo clean` |
| 生成成品 | `npx hexo generate` |
| 发布上线 | `npx hexo deploy` |
| 生成+上线一步到位 | `npx hexo deploy --generate` |
| 备份源码 | `git add -A; git commit -m "说明"; git push` |
| 查看改动 | `git status` |
| 查看历史 | `git log --oneline -5` |